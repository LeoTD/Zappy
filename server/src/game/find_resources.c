#include "server.h"
#include "tile_type.h"

char	*find_food(t_tile *tile)
{
	char	*count;
	char	*food;

	food = "food";
	count = itoa(tile->food);
	return ("food");
}

char	*find_stone(t_tile *tile)
{
	return ("stone");
}

char	*find_stones(t_tile *tile)
{
	return ("stones");

}
